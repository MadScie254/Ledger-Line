{"step_index":3694,"source":"USER_EXPLICIT","type":"USER_INPUT","status":"DONE","created_at":"2026-08-20T21:33:13Z","content":"<USER_REQUEST>\nRemaining Fixes, Free API Integrations, Feature Roadmap\nBuilds on ledgerline-copilot-master-prompt.md. Verified against the repo and live Supabase project (kqbzstklmhxfcslcigti) on Aug 21 2026. Paste into Copilot Chat. Do Section 1 (fixes) first — they're small and low-risk. Section 2 (APIs) and Section 3 (features) can run in parallel after that.\n0. Confirmed since Phase 1 (context for Copilot, not action items)\nReal, ledger-connected pages went from ~20 to 34/58 routes. Trial Balance, Balance Sheet, Cash Flow, AR/AP Aging, Payroll Run/Payslips, Close Books, Bill Payments, and Banking (Connections/Reconcile/Rules) are now genuinely wired. The P&L casing bug (ledger.ts using lowercase account types against an uppercase enum) is fixed. The org switcher, toast system (sonner), and CI pipeline (.github/workflows/ci.yml) are all real and working. The tenant-isolation fix from Phase 1 (app_metadata instead of user_metadata for orgId) is holding correctly.\n1. Fixes (small, do these first)\n1.1 — .env.example missing browser-side Supabase vars\napp/(auth)/login/page.tsx and signup/page.tsx call createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!). .env.example only lists the server-side SUPABASE_URL / SUPABASE_PUBLISHABLE_KEY / SUPABASE_SECRET_KEY. Add:\nNEXT_PUBLIC_SUPABASE_URL=\"https://your-project.supabase.co\" NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=\"your-anon-key\"\nAlso add these two (as dummy values) to the build step env block in .github/workflows/ci.yml, next to the existing SUPABASE_URL/SUPABASE_SECRET_KEY dummies, so CI actually catches this class of bug in future.\n1.2 — wrangler.toml placeholder IDs\nBoth Hyperdrive and KV bindings still read id = \"00000000000000000000000000000000\". Not urgent for local dev, but block your first real Cloudflare deploy on this — create the actual Hyperdrive config and KV namespace via wrangler and paste the real IDs in.\n1.3 — Remove committed debug artifacts\nDelete packages/db/drift.txt, packages/db/fix_drift.sql, scratch/fix-group-a.js, scratch/fix-group-b.js, packages/db/prisma/pulled.prisma, and the two dev-server.*.log files at the repo root. drift.txt is a raw failed npm-command log that also exposes your local Windows path (C:\\Users\\MadScie254\\Documents\\...) — not a security issue, just noise. Add *.log, drift.txt, pulled.prisma, and /scratch to .gitignore so this doesn't recur.\n1.4 — Tighten the RLS orgId fallback\npackages/db/prisma/migrations/20260812000004_rls_policies/migration.sql defines:\nSELECT coalesce( (current_setting('request.jwt.claims', true)::jsonb -> 'app_metadata' ->> 'orgId'), (current_setting('request.jwt.claims', true)::jsonb -> 'user_metadata' ->> 'orgId'), '' )\nNow that onboarding reliably sets app_metadata.orgId via the admin client, drop the user_metadata fallback — it's the same category of trust issue you already fixed at the application layer, just dormant in the DB layer (currently inert only because Prisma's direct connection bypasses RLS entirely; it becomes live risk the moment anything queries Supabase via the client SDK with a user JWT, e.g. a future real-time subscription). New migration:\nCREATE OR REPLACE FUNCTION public.ledgerline_org_id() RETURNS text LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$ SELECT coalesce( (current_setting('request.jwt.claims', true)::jsonb -> 'app_metadata' ->> 'orgId'), '' ) $$;\n1.5 — \"New\" button and remaining dead-end pages\nThe global New button in app-shell.tsx always routes to /sales/invoices. Turn it into a small dropdown (Invoice / Bill / Expense / Customer / Vendor), same pattern as the org switcher popover you already built. Then work through the remaining placeholder pages — same priority logic as Phase 1's Section 2 table, updated for what's left:\nPriority\nRoutes\nNotes\nHigh\nsales/sales-receipts, sales/sales-orders\nYou already have the exact posting pattern for SalesReceipt (immediate cash sale) from the Invoice/Payment code\nHigh\nnotifications\nLow effort, high visibility — the Bell button already routes here\nMedium\ncustomer-hub/leads, follow-ups\nExtend Customer with a stage field rather than a new model\nMedium\ntax/etims, tax/center, tax/filing-calendar\nKenya-specific — see Section 3.3\nMedium\nexpenses/purchase-orders, inventory/adjustments, reorder-alerts\nLow\nsettings/billing, settings/templates, team/accountant-access, projects*, sales/overview, sales/recurring, accounting/reconciliation-history\nLower traffic, do last\n2. Free / open-source API integrations\n2.1 — Multi-currency: Frankfurter (do this first — your ExchangeRate table has 0 rows despite the UI already offering 6 currencies at onboarding)\nWhy Frankfurter: genuinely open-source, no API key, no rate limit published, ECB-sourced, covers KES. Good enough for daily accounting FX — you don't need intraday rates for journal posting.\n// apps/web/lib/exchange-rates.ts export async function fetchDailyRates(base: string, targets: string[]) { const res = await fetch( `https://api.frankfurter.dev/v1/latest?base=${base}&symbols=${targets.join(\",\")}` ); if (!res.ok) throw new Error(`Frankfurter fetch failed: ${res.status}`); const data = await res.json() as { date: string; rates: Record<string, number> }; return data; }\nAdd a scheduled route (app/api/cron/exchange-rates/route.ts) that runs once daily (Cloudflare Cron Trigger or a GitHub Actions scheduled workflow hitting the endpoint), fetches rates for every currency any org actually uses, and upserts into ExchangeRate (date, baseCurrency, quoteCurrency, rate).\nWire settings/currencies (currently a dead placeholder) to read from this table instead of nothing.\nWhen an invoice/bill is created in a non-base currency, look up the nearest ExchangeRate row and post the FX-adjusted amount — this is what actually makes the \"6 currencies at onboarding\" promise real instead of a dead end.\nNote: Frankfurter has no SLA. Cache the day's rates in ExchangeRate and never call it more than once a day per currency — you already have the table, use it as the cache.\n2.2 — Business news / \"Business Feed\" enrichment\nYour business-feed module already exists and is real (BusinessFeedWorkspace) but is fixture/insight-based only, not pulling any external news. Two free, genuinely production-safe options — do not use NewsAPI.org or GNews free tiers, both explicitly forbid commercial/production use:\nPrimary — Google News RSS (free, unlimited, no key):\n// apps/web/lib/business-news.ts import { XMLParser } from \"fast-xml-parser\"; // npm install fast-xml-parser export async function fetchKenyaBusinessNews(query = \"Kenya business\") { const url = `https://news.google.com/rss/search?q=${encodeURIComponent(query)}&hl=en-KE&gl=KE&ceid=KE:en`; const res = await fetch(url); const xml = await res.text(); const parsed = new XMLParser().parse(xml); const items = parsed.rss.channel.item as Array<{ title: string; link: string; pubDate: string; source: string }>; return items.slice(0, 10); }\nSecondary (if you want images/structured fields later) — NewsData.io free tier: 200 credits/day (~2,000 articles), commercial use explicitly permitted, no credit card. https://newsdata.io/api/1/news?apikey=KEY&q=Kenya%20business&language=en.\nSurface this as a \"Market pulse\" card on business-feed or the dashboard — 5 headlines, cached and refreshed a few times a day (respect the source, don't poll per page-load).\n2.3 — Stocks/NSE: be upfront with users about what's real\nThere is no legitimate free API for live Nairobi Securities Exchange data — the official feed is licensed through ICE Data Services, and none of the usual free-tier providers (Alpha Vantage, Finnhub, Twelve Data) cover NSE Kenya. Scraping nse.co.ke would violate their terms and break on every layout change.\nRecommendation: don't build a \"stocks\" feature for v1. It's low-relevance for SME bookkeeping users and every path to it is either paid (mystocks.africa's licensed NSE API, budget for later) or fragile (scraping). If you want some market-context widget for the dashboard, use Frankfurter for KES/USD/EUR movement (you're already pulling it for Section 2.1) rather than stocks — that's the number your users actually care about.\n2.4 — Kenya-specific data worth wiring instead (higher value than stocks)\nKRA PIN format validation — client-side regex (A\\d{9}[A-Z] pattern) on every form that already collects a PIN (Employee, Vendor, Customer, Company Settings) — no API needed, just correctness.\nPublic holidays (Kenya) — for payroll/run date logic and tax/filing-calendar, use the free, open [Nager.Date API](https://date.nager.at/) (/api/v3/PublicHolidays/2026/KE), no key required.\n3. Feature & UI additions worth prioritizing\nCommand palette (Cmd/Ctrl+K) — you already render a decorative ⌘ in the global search input from Phase 1; still not wired. Fuzzy-search navigation.ts plus recent records (last 5 invoices/customers/vendors touched).\nM-Pesa Daraja STK push on sales/payments-received — given your existing Daraja experience and Kenya-market focus, this is the single highest-impact feature left on the table, bigger than any remaining placeholder page.\neTIMS bridge (tax/etims) — currently an empty state; this is a genuine differentiator for Kenyan SMEs if built even minimally (manual invoice submission log against KRA's eTIMS, before attempting real-time API integration).\nNotification content, not just the empty shell — now that notifications has a real Bell button pointing at it, give it something to show: overdue invoice reminders, low-stock alerts (ties to inventory/reorder-alerts), and close-books reminders, generated server-side from data you already have.\nDashboard: cash-flow trend line + expense-by-category donut — both derivable from JournalLine grouped by Account.type/code, same query shape as the P&L route. You have the data (34 balanced journal entries); the dashboard just isn't charting it yet beyond KPIs.\nReport export (CSV/PDF) — Trial Balance, P&L, Balance Sheet are all real now; none can be exported. This is usually the first thing an accountant asks for once a report is real.\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-08-21T00:33:13+03:00.\n\nThe user's current state is as follows:\nActive Document: c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\scratch\\fix-group-a.js (LANGUAGE_JAVASCRIPT)\nCursor is on line: 1\nOther open documents:\n- c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\apps\\web\\components\\dashboard-charts.tsx (LANGUAGE_TSX)\n- c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\apps\\web\\lib\\workspace.ts (LANGUAGE_TYPESCRIPT)\n- c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\packages\\db\\prisma\\schema.prisma (LANGUAGE_UNSPECIFIED)\n- c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\scratch\\fix-group-a.js (LANGUAGE_JAVASCRIPT)\n- c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line\\scratch\\fix-group-b.js (LANGUAGE_JAVASCRIPT)\nRunning terminal commands:\n- npm run dev (in c:\\Users\\MadScie254\\Documents\\GitHub\\Ledger-Line, running for 35m57s)\n</ADDITIONAL_METADATA>"}
